# Phase 4 P1 R6000 Report — BF16 + NVFP4 v8-RTN

Date: 2026-05-14

Machine: RTX PRO 6000 Blackwell / R6000

Reference workload:

- BF16 oracle:
  `/root/autodl-tmp/models/Lynn-V4-Pro-Distill-Qwen-35B-A3B-BF16-merged`
- NVFP4 v8-RTN input:
  `/root/autodl-tmp/models/Lynn-V4-Pro-Distill-Qwen-35B-A3B-NVFP4-v8-RTN`
- Lynn engine repo:
  `/root/autodl-tmp/lynn-engine`

## Verdict

**P1 PASS.**

Lynn engine can independently inspect the Lynn V4 Pro BF16 checkpoint, load
BF16 layer 0 through its own loader, inspect the NVFP4 v8-RTN checkpoint, and
fail loudly on unsupported NVFP4 instead of silently producing corrupted
weights.

This does **not** depend on SGLang, vLLM, TRT-LLM, llama.cpp, or any other
inference engine. The only external libraries used in this P1 pass are
metadata/file readers (`safetensors`, `torch`) plus Lynn engine's own loader and
manifest code.

**P2 PREP PASS.**

The first target tensor has a clean BF16 oracle and a matching v8-RTN four-tensor
group. Packed shape, scale rows, and group size all line up for slow-path
dequant work.

**P2-A SINGLE TENSOR DEQUANT PASS.**

After P1, Lynn engine added a slow, explicit NVFP4 v8-RTN dequant path and ran it
against the same target tensor. Lynn's implementation matches
`compressed_tensors` reference dequant exactly (`mean_abs=0`, `max_abs=0`), and
the resulting tensor tracks the BF16 oracle closely (`cosine=0.99655`,
`sign_match=1.0`).

**P2-A2 SINGLE LINEAR FORWARD PASS.**

Using a deterministic random input (`16 x 2048`, seed `20260514`), the BF16
oracle Linear and the NVFP4 slow-dequant Linear produce highly aligned outputs:
`cosine=0.99567`, `relative_l2=0.09307`, `sign_match=0.96405`.

**P2-A3 LAYER-0 LINEAR COVERAGE PASS.**

The single-tensor primitive was expanded to 23 representative layer-0 Linear
modules: all linear-attention packed linears, all shared-expert linears, and
five representative MoE experts (`0, 1, 42, 127, 255`) across `gate/up/down`.
All 23 passed. The runner also inferred Lynn's BF16 fused MoE layout:
`mlp.experts.gate_up_proj[e] == concat(gate_proj, up_proj)`.

Summary:

| Metric | Value |
|---|---:|
| covered Linear items | `23` |
| pass / fail | `23 / 0` |
| inferred gate/up order | `gate_up` |
| min weight cosine | `0.99535` |
| min forward cosine | `0.99469` |

**P2-A4 NVFP4 LAYER LOADER PASS.**

`load_qwen36_layer()` now supports compressed-tensors v8-RTN NVFP4 through the
slow dequant path. Loading layer 0 from the v8-RTN checkpoint returns the same
18 short keys as the BF16 loader, with no missing keys, no extra keys, and no
shape mismatches.

Summary:

| Metric | Value |
|---|---:|
| BF16 loader keys | `18` |
| NVFP4 loader keys | `18` |
| missing keys | `0` |
| extra keys | `0` |
| shape mismatches | `0` |
| pass / warn | `18 / 0` |
| min cosine across comparable tensors | `0.99535` |

This is the first true Lynn engine NVFP4 layer startup on R6000: one public
`load_qwen36_layer()` entrypoint can now load BF16 or v8-RTN NVFP4 and normalize
both into the same fused BF16 layout expected by existing forward code.

**P2-A5 LAYER-0 FORWARD PASS.**

The normalized BF16 and NVFP4 layer-0 weights now both run through Lynn engine's
own `_layer_forward()` path without SGLang, vLLM, TRT-LLM, llama.cpp, or
Transformers model execution. The benchmark uses a deterministic hidden state
(`1 x 8 x 2048`, seed `20260514`) and compares the NVFP4 slow-dequant output
against the BF16 oracle after the full layer block: linear attention, residual,
RMSNorm, MoE routing, shared expert, and final residual.

Summary:

| Metric | Value |
|---|---:|
| layer type | `linear_attention` |
| input shape | `[1, 8, 2048]` |
| output shape | `[1, 8, 2048]` |
| mean abs error | `0.00702` |
| max abs error | `0.23698` |
| relative L2 | `0.01051` |
| output cosine | `0.99994` |
| sign match | `0.99750` |

This is the first end-to-end Lynn engine layer-level proof that v8-RTN NVFP4
can be loaded, dequantized, routed through the fused MoE layout, and executed
against the BF16 oracle on R6000.

**P2-B MULTI-LAYER FORWARD PASS.**

The layer-level proof was extended into a sequential multi-layer chain. Lynn
engine loads each layer independently from BF16 and v8-RTN NVFP4, runs both
hidden states through the same `_layer_forward()` implementation, and compares
the running hidden state after every layer.

Four gates passed:

| Gate | Coverage | Min / Final Cosine | Verdict |
|---|---|---:|---:|
| layers `0..1` | two `linear_attention` blocks | `0.99991` | PASS |
| layers `0..3` | three `linear_attention` blocks + one `full_attention` block | `0.99937` | PASS |
| layers `0..7` | six `linear_attention` blocks + two `full_attention` blocks | `0.99888` | PASS |
| layers `0..11` | nine `linear_attention` blocks + three `full_attention` blocks | `0.99860` | PASS |

The `0..11` run is the first Lynn engine NVFP4 extended mixed-attention-chain
proof: it covers repeated linear attention, repeated full attention, RoPE/GQA,
MoE routing, shared expert, residuals, and RMSNorm without using any external
inference engine.

**P2-C FULL-MODEL PREFILL / LOGITS PASS.**

The public BF16 and v8-RTN checkpoints now run through Lynn engine's complete
40-layer prefill path. The benchmark streams one layer at a time, loads outside
weights directly from HF-style safetensors (`embed_tokens`, `norm`, `lm_head`),
and compares final logits from NVFP4 slow-dequant against the BF16 oracle for
the prompt `你好`.

Summary:

| Metric | BF16 | NVFP4 v8-RTN |
|---|---:|---:|
| full prefill wall time | `14.2s` | `40.3s` |
| layers executed | `40 / 40` | `40 / 40` |
| external inference engines | `0` | `0` |

Logit comparison:

| Metric | Value |
|---|---:|
| mean abs error | `0.14927` |
| max abs error | `0.98438` |
| relative L2 | `0.09023` |
| logit cosine | `0.99591` |
| top-10 overlap | `0.90` |
| top-1 match | `False` |

`top1_match=False` is acceptable for this startup gate because the top logits
are tightly clustered; the goal here is proving end-to-end executable logits
with high distribution-level agreement, not bit-identical greedy generation.

**P2-D MINIMAL GENERATION PARITY PASS.**

The final overnight gate exercises the brute-force greedy generation loop rather
than only comparing prefill logits. For prompt `用一句话解释MoE:` and
`max_new=4`, BF16 and NVFP4 v8-RTN produce identical token IDs and identical
text through Lynn engine's own 40-layer resident generation path.

| Metric | Value |
|---|---:|
| prompt | `用一句话解释MoE:` |
| max new tokens | `4` |
| BF16 new IDs | `[220, 96125, 97998, 96067]` |
| NVFP4 new IDs | `[220, 96125, 97998, 96067]` |
| token match rate | `1.0` |
| output text | `用一句话解释MoE: 一个团队里` |

**P2-E INCREMENTAL DECODE PARITY PASS.**

After the brute-force generation gate, Lynn engine's cache/recurrent-state
decode path was updated to support the fused expert layout used by the public
BF16 and v8-RTN loaders. BF16 and NVFP4 now both run prefill once and then use
incremental decode for each subsequent token.

For prompt `用一句话解释MoE:` and `max_new=4`, incremental BF16 and incremental
NVFP4 v8-RTN generate the same token IDs as the brute-force gate:

| Metric | Value |
|---|---:|
| BF16 incremental IDs | `[220, 96125, 97998, 96067]` |
| NVFP4 incremental IDs | `[220, 96125, 97998, 96067]` |
| token match rate | `1.0` |
| BF16 decode step latency | `~75-105ms` |
| NVFP4 decode step latency | `~74-75ms` |

This gate covers full-attention KV cache, linear-attention recurrent state,
decode-time MoE routing, fused expert layout, and NVFP4 slow-dequant resident
weights.

**P2-F MULTI-PROMPT INCREMENTAL GATE WARN.**

The same incremental path was expanded to four prompts. Three prompts match
BF16 exactly, and one prompt branches into a reasonable alternative
continuation rather than garbage:

| Metric | Value |
|---|---:|
| prompts | `4` |
| exact prompt matches | `3 / 4` |
| token match rate | `0.8125` |
| verdict | `WARN` |

The only non-exact prompt is a semantic branch:

| Prompt | BF16 continuation | NVFP4 continuation |
|---|---|---|
| `比较RoPE和ALiBi:` | ` 给Transformer加` | ` 从位置编码` |

**P2-G RESIDENT MULTI-PROMPT GATE WARN.**

The first reusable Lynn engine session layer is now in place. It loads BF16
once, runs the four-prompt suite, releases it, then loads NVFP4 once and runs
the same suite. This avoids per-prompt weight reloads and measures steady-state
incremental decode:

| Metric | Value |
|---|---:|
| BF16 resident load | `13.74s` |
| NVFP4 resident load | `38.22s` |
| prompts | `4` |
| exact prompt matches | `3 / 4` |
| token match rate | `0.8125` |
| resident decode speed | `~13.1 tok/s` |
| verdict | `WARN` |

This is the current P2 boundary: Lynn engine independently runs BF16 and NVFP4
through the same resident incremental decode path. Remaining work is
quantization-noise analysis, broader prompt coverage, and native FP4 kernel
performance.

**P2-H RESIDENT LOGIT ANALYSIS COMPLETE.**

The `WARN` in P2-F/P2-G is not a loader failure. A top-k logit diagnostic shows
the first three prompts have high top-k overlap and exact token parity. The
`RoPE/ALiBi` prompt diverges after a close-margin second token:

| Prompt | Exact | Notes |
|---|---:|---|
| `用一句话解释MoE:` | `True` | top-k overlap `0.8-0.9` |
| `If a train travels...` | `True` | top-k overlap `0.9-1.0` |
| `Python递归阶乘函数:` | `True` | top-k overlap `0.9-1.0` |
| `比较RoPE和ALiBi:` | `False` | step 1 BF16 margin `0.4375`; NVFP4 top-1 is BF16 rank 2 |

For the divergent prompt, BF16 continues `给Transformer加`, while NVFP4
continues `从位置编码`. Both are coherent continuations. Once that near-tie token
branches, autoregressive context causes later top-k overlap to drop. This is
quantization noise amplification, not random expert initialization.

**P2-I RESIDENT CLI ENTRY PASS.**

A minimal CLI entry now wraps the resident runner. This is the bridge between
benchmark scripts and a future HTTP server: load one model, run one or more
prompts, emit JSON.

Command shape:

```bash
python benchmarks/resident_cli.py \
  --model /path/to/BF16-or-NVFP4 \
  --prompt "用一句话解释MoE:" \
  --max-new 8 \
  --top-k 5 \
  --out /tmp/resident_cli.json
```

R6000 smoke:

| Model | Load | Output prefix | Decode TPS |
|---|---:|---|---:|
| BF16 merged | `26.24s` | `用一句话解释MoE: 一个团队里，每个专家只` | `12.80` |
| NVFP4 v8-RTN | `40.59s` | `用一句话解释MoE: 一个团队里，每个专家只` | `12.62` |

This proves both released formats use the same first-class Lynn engine CLI
entry without SGLang, vLLM, TRT-LLM, or llama.cpp.

**P2-J OPENAI-COMPAT HTTP ENTRY PASS.**

The existing `server.openai_http` server now reuses `LynnIncrementalRunner`
instead of maintaining a second decode implementation. NVFP4 v8-RTN starts a
local OpenAI-compatible server on R6000 and returns normal `content` fields from
`/v1/chat/completions` with server-side no-think defaulting.

R6000 smoke:

| Endpoint | Prompt | Output prefix | E2E TPS |
|---|---|---|---:|
| `/health` | - | `{"status":"ok"}` | - |
| `/v1/chat/completions` | `用一句话解释MoE active parameters` | `MoE 的 active parameters 指的是` | `4.48` |
| `/v1/completions` | `Python递归阶乘函数:` | `def factorial(n): if n` | `6.48` |

This is not a production server yet: requests are serialized, batching is not
implemented, and native FP4 GEMM is still future work. But it proves the engine
can serve BF16/NVFP4 through its own HTTP entry without third-party inference
engines.

**P2-K BF16 HTTP ENTRY PASS.**

The same HTTP entry also serves the BF16 merged checkpoint:

| Endpoint | Prompt | Output prefix | E2E TPS |
|---|---|---|---:|
| `/health` | - | `{"status":"ok"}` | - |
| `/v1/chat/completions` | `用一句话解释MoE active parameters` | `MoE 的 active parameters 指的是` | `4.55` |
| `/v1/completions` | `Python递归阶乘函数:` | `def factorial(n): if n` | `8.13` |

BF16 resident load for the HTTP server was `18.6s`. Together with P2-J, this
closes the P2 serving loop for both public release formats: BF16 and
NVFP4-v8-RTN use the same Lynn engine HTTP entry.

## Checks

| Check | Result | Evidence |
|---|---:|---|
| BF16 manifest scan | PASS | `bf16_or_unquantized`, 1026 tensors, 16 files |
| BF16 dtype integrity | PASS | `{'BF16': 1026}` |
| BF16 Lynn loader layer 0 | PASS | `load_qwen36_layer(..., layer=0)` returned 18 tensors |
| v8-RTN manifest scan | PASS | `compressed_tensors_nvfp4_v8_rtn`, 124,756 tensors, 1 file |
| v8-RTN quant config | PASS | `quant_method=compressed-tensors`, `format=nvfp4-pack-quantized` |
| v8-RTN fail-loud guard | PASS | loader raises `NotImplementedError` instead of silent garbage |
| P2 single-tensor layout prep | PASS | BF16 shape and v8 unpacked shape match; group size is 16 |
| P2 single-tensor slow dequant | PASS | Lynn dequant equals `compressed_tensors` reference exactly |
| P2 single-Linear forward | PASS | NVFP4 dequant Linear output tracks BF16 oracle output |
| P2 layer-0 Linear coverage | PASS | 23 representative Linear modules all pass |
| P2 NVFP4 layer loader startup | PASS | `load_qwen36_layer(v8, 0)` returns the same 18-key fused layout as BF16 |
| P2 layer-0 forward | PASS | NVFP4 slow-dequant `_layer_forward()` output tracks BF16 oracle output |
| P2 multi-layer forward | PASS | layers `0..3` mixed-attention chain tracks BF16 oracle output |
| P2 full-model prefill/logits | PASS | 40-layer NVFP4 slow-dequant logits track BF16 oracle logits |
| P2 minimal generation parity | PASS | BF16 and NVFP4 generate identical 4-token continuation |
| P2 incremental decode parity | PASS | BF16 and NVFP4 cache decode generate identical 4-token continuation |
| P2 multi-prompt incremental gate | WARN | 3/4 exact prompt matches, 0.8125 token match rate |
| P2 resident runner gate | WARN | BF16 and NVFP4 reusable sessions run the same 4-prompt suite at ~13.1 tok/s |
| P2 resident logit analysis | COMPLETE | divergence traced to close-margin top-k branch, not loader corruption |
| P2 resident CLI entry | PASS | BF16 and NVFP4 both run through `benchmarks/resident_cli.py` |
| P2 OpenAI HTTP entry | PASS | NVFP4 serves `/v1/chat/completions` and `/v1/completions` via Lynn engine |
| P2 BF16 HTTP entry | PASS | BF16 serves the same OpenAI-compatible endpoints via Lynn engine |

## BF16 Manifest Summary

| Field | Value |
|---|---:|
| quant format | `bf16_or_unquantized` |
| total tensors | 1026 |
| file count | 16 |
| dtype counts | `{'BF16': 1026}` |

BF16 layer 0 loader smoke:

```text
LYNN_BF16_LAYER0_LOAD_OK 18 {'num_experts': 256, 'shared_intermediate': 512}
```

Layer 0 sample keys included:

- `linear_attn.in_proj_qkv.weight`
- `mlp.experts.down_proj`
- `mlp.experts.gate_up_proj`

## v8-RTN Manifest Summary

| Field | Value |
|---|---:|
| quant format | `compressed_tensors_nvfp4_v8_rtn` |
| quant method | `compressed-tensors` |
| checkpoint format | `nvfp4-pack-quantized` |
| total tensors | 124,756 |
| file count | 1 |

Suffix distribution:

| Suffix | Count |
|---|---:|
| `.weight_packed` | 31,030 |
| `.weight_scale` | 31,030 |
| `.weight_global_scale` | 31,030 |
| `.input_global_scale` | 31,030 |
| `.weight` | 410 |
| `.bias` | 166 |

Dtype distribution:

| Dtype | Count |
|---|---:|
| `BF16` | 636 |
| `F32` | 62,060 |
| `U8` | 31,030 |
| `F8_E4M3` | 31,030 |

## P2 Target Tensor

Logical tensor:

```text
model.language_model.layers.0.linear_attn.in_proj_qkv.weight
```

Tensor group:

| Tensor | Dtype | Shape |
|---|---|---:|
| BF16 `.weight` | `BF16` | `[8192, 2048]` |
| v8 `.weight_packed` | `U8` | `[8192, 1024]` |
| v8 `.weight_scale` | `F8_E4M3` | `[8192, 128]` |
| v8 `.weight_global_scale` | `F32` | `[1]` |
| v8 `.input_global_scale` | `F32` | `[1]` |

Derived checks:

| Check | Value |
|---|---:|
| expected unpacked shape | `[8192, 2048]` |
| expected unpacked shape matches BF16 | `True` |
| `weight_packed.last_dim * 2 == bf16.last_dim` | `True` |
| scale rows match output dim | `True` |
| inferred group size | `16` |
| group size matches NVFP4 expected value | `True` |

## Why This Matters

The most dangerous failure mode found earlier was silent corruption: packed
`uint8` weights and FP8 scale tensors could be interpreted as ordinary weights
and allow a model to "load" while producing garbage.

P1 closes that class of failure for this workload:

- BF16 known-good path loads through Lynn engine.
- NVFP4 v8-RTN is recognized as a supported target format but not silently
  executed before dequant support exists.
- The next tensor-level work item is fully specified and has a BF16 oracle.

This is the 0-to-1 step for Lynn engine on R6000: it can now distinguish
"known-good BF16", "known NVFP4 format awaiting P2 dequant", and "unsafe to
run".

## Artifacts

Remote artifacts on R6000:

```text
/root/autodl-tmp/reports/lynn-engine-p1/bf16_manifest.json
/root/autodl-tmp/reports/lynn-engine-p1/v8_manifest.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_single_tensor_prep.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_single_tensor_dequant.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_layer0_coverage.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_loader_layer0_compare.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_layer0_forward.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_layers0_1_forward.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_layers0_3_forward.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_layers0_7_forward.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_layers0_11_forward.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_full_model_prefill.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_generate_parity.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_incremental_parity.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_incremental_multi_prompt.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_resident_multi_prompt.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_resident_logit_analysis.json
/root/autodl-tmp/reports/lynn-engine-p1/resident_cli_bf16.json
/root/autodl-tmp/reports/lynn-engine-p1/resident_cli_nvfp4.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_openai_http_nvfp4_smoke.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_openai_http_bf16_smoke.json
/root/autodl-tmp/reports/lynn-engine-p1/overnight_report.md
```

Local implementation files:

```text
engine/quant_manifest.py
engine/dequant.py
engine/loader.py
engine/full_forward.py
engine/resident_runner.py
server/openai_http.py
benchmarks/quant_manifest_probe.py
benchmarks/p2_nvfp4_single_tensor.py
benchmarks/p2_nvfp4_layer0_coverage.py
benchmarks/p2_nvfp4_layer0_forward.py
benchmarks/p2_nvfp4_multilayer_forward.py
benchmarks/p2_full_model_prefill.py
benchmarks/p2_generate_parity.py
benchmarks/p2_incremental_parity.py
benchmarks/p2_incremental_multi_prompt.py
benchmarks/p2_resident_multi_prompt.py
benchmarks/p2_resident_logit_analysis.py
benchmarks/resident_cli.py
```

## Next Step: P2

The first tensor is already through slow-path dequant:

```text
model.language_model.layers.0.linear_attn.in_proj_qkv.weight
```

Result:

| Comparison | Value |
|---|---:|
| Lynn dequant vs `compressed_tensors` mean abs | `0.0` |
| Lynn dequant vs `compressed_tensors` max abs | `0.0` |
| Lynn dequant vs BF16 oracle mean abs | `0.00103997` |
| Lynn dequant vs BF16 oracle max abs | `0.03125` |
| Lynn dequant vs BF16 oracle relative L2 | `0.09531` |
| Lynn dequant vs BF16 oracle cosine | `0.99655` |
| Lynn dequant vs BF16 oracle sign match | `1.0` |

The first layer-level forward gate is also through:

| Comparison | Value |
|---|---:|
| NVFP4 layer output vs BF16 oracle mean abs | `0.00702` |
| NVFP4 layer output vs BF16 oracle max abs | `0.23698` |
| NVFP4 layer output vs BF16 oracle relative L2 | `0.01051` |
| NVFP4 layer output vs BF16 oracle cosine | `0.99994` |
| NVFP4 layer output vs BF16 oracle sign match | `0.99750` |

The multi-layer mixed-attention gates are also through:

| Comparison | Value |
|---|---:|
| layers `0..3` final cosine | `0.99937` |
| layers `0..3` final relative L2 | `0.03537` |
| layers `0..7` final cosine | `0.99888` |
| layers `0..7` final relative L2 | `0.04740` |
| layers `0..7` final sign match | `0.98474` |
| layers `0..11` final cosine | `0.99860` |
| layers `0..11` final relative L2 | `0.05321` |
| layers `0..11` final sign match | `0.98145` |

The full-model prefill/logits gate is through:

| Comparison | Value |
|---|---:|
| BF16 full prefill | `14.2s` |
| NVFP4 full prefill | `40.3s` |
| logit cosine | `0.99591` |
| logit relative L2 | `0.09023` |
| top-10 overlap | `0.90` |

The first generation parity gate is through:

| Comparison | Value |
|---|---:|
| prompt | `用一句话解释MoE:` |
| new tokens | `4` |
| BF16 continuation | ` 一个团队里` |
| NVFP4 continuation | ` 一个团队里` |
| exact token match | `True` |

The first incremental decode parity gate is also through:

| Comparison | Value |
|---|---:|
| BF16 incremental continuation | ` 一个团队里` |
| NVFP4 incremental continuation | ` 一个团队里` |
| exact token match | `True` |
| decode path | `KV cache + linear-attention recurrent state` |

Single Linear forward probe:

| Comparison | Value |
|---|---:|
| input shape | `[16, 2048]` |
| output shape | `[16, 8192]` |
| NVFP4 Linear vs BF16 Linear mean abs | `0.04989` |
| NVFP4 Linear vs BF16 Linear max abs | `0.89498` |
| NVFP4 Linear vs BF16 Linear relative L2 | `0.09307` |
| NVFP4 Linear vs BF16 Linear cosine | `0.99567` |
| NVFP4 Linear vs BF16 Linear sign match | `0.96405` |

Remote artifact:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p2_single_tensor_dequant.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_layer0_coverage.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_loader_layer0_compare.json
```

The next P2 step is no longer "figure out how to decode NVFP4". It is to expand
the same proven dequant primitive from one tensor to one full Linear group, then
one layer.

Original P2 slow-path dequant sequence:

P2 slow-path dequant sequence:

1. unpack `uint8` packed bytes into two FP4 values per byte,
2. decode FP4 values using the NVFP4 value table,
3. broadcast `weight_scale` with group size 16,
4. multiply by `weight_global_scale`,
5. compare dequantized FP32/BF16 statistics against the BF16 oracle.

Only after one tensor passes should we expand to one Linear, one layer, then
multi-prompt parity. Production tensor-core NVFP4 GEMM remains out of scope
until the resident oracle and packed bridge gates are green.

## 2026-05-14 Addendum — P2 HTTP Surface + P3-A Native Packed Kernel

The P2 independent-serving surface is now through for both checkpoint formats:

| Gate | Result |
|---|---:|
| BF16 OpenAI-compatible HTTP smoke | PASS |
| NVFP4 v8-RTN OpenAI-compatible HTTP smoke | PASS |
| dependency on SGLang/vLLM/TRT/llama.cpp | none |

Remote reports:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p2_openai_http_bf16_smoke.json
/root/autodl-tmp/reports/lynn-engine-p1/p2_openai_http_nvfp4_smoke.json
```

P3-A also has its first native packed-weight kernel:

| Comparison | Value |
|---|---:|
| tensors | QKV + expert gate/up/down |
| device | `RTX PRO 6000 Blackwell Server Edition (sm_120)` |
| packed NVFP4 kernel vs resident dequant cosine | `1.000000119` |
| packed NVFP4 kernel vs resident dequant relative L2 | `1.80e-7` |
| packed NVFP4 kernel max abs | `1.43e-6` |
| packed NVFP4 kernel latency | `0.0527 ms` |
| resident dequantized linear latency | `0.0208 ms` |
| materialize-dequant-each-call latency | `0.640 ms` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_native_nvfp4_matvec_probe.json
/root/autodl-tmp/reports/lynn-engine-p1/p3_native_nvfp4_matvec_multi_probe.json
```

Representative multi-probe results:

| Tensor | Cosine | rel_l2 | Packed ms | Materialize-each-call ms |
|---|---:|---:|---:|---:|
| linear_attn.in_proj_qkv | `1.000000119` | `1.80e-7` | `0.0527` | `0.645` |
| expert0.gate_proj | `0.999999881` | `2.17e-7` | `0.0301` | `0.205` |
| expert0.up_proj | `1.000000000` | `2.03e-7` | `0.0299` | `0.203` |
| expert0.down_proj | `1.000000119` | `1.47e-7` | `0.0230` | `0.201` |

Interpretation:

- The new Triton kernel consumes `weight_packed + weight_scale +
  weight_global_scale` directly and does not materialize a full BF16 weight
  matrix for this linear.
- It is already about 12x faster than dequantizing the tensor every call, but
  it is still slower than resident BF16 matmul. That is expected: this is a
  scalar bridge kernel, not the final tensor-core FP4 GEMM.
- The useful milestone is the runtime contract: Lynn Engine can now pass packed
  NVFP4 tensors into a native kernel. P3-B should wrap more Linear sites with
  this contract; the later FP4 GEMM work can swap the kernel internals without
  changing loader semantics.

## P3-B — Runtime Wrapper Contract

`PackedNVFP4Linear` now exposes the packed kernel as an engine runtime object.
It accepts single-token inputs shaped `[D]`, `[1, D]`, or `[1, 1, D]` and
returns the same leading shape as `F.linear`.

| Probe | Value |
|---|---:|
| tensor | `model.language_model.layers.0.linear_attn.in_proj_qkv.weight` |
| wrapper vs resident reference cosine | `1.000000000` |
| wrapper vs resident reference rel_l2 | `8.12e-9` |
| `[1, D]` vs `[D]` max_abs | `0.0` |
| `[1, 1, D]` vs `[D]` max_abs | `0.0` |
| wrapper latency | `0.055 ms` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_runtime_wrapper_probe.json
```

This keeps P2 resident dequant untouched while giving decode-path code a stable
object to call. The next integration step is to route selected decode linear
sites through `PackedNVFP4Linear`; the next performance step is replacing the
inner scalar kernel with a tensor-core FP4 GEMM.

## P3-C — Decode-Path QKV Integration

`engine.incremental_decode` now dispatches through a small `_linear()` helper:
regular tensors still call `F.linear`, while `PackedNVFP4Linear` calls the
native packed-weight kernel. This keeps the P2 resident path intact and lets us
swap individual decode projections one at a time.

First integration probe replaces only:

```text
linear_attn.in_proj_qkv.weight
```

for layer 0 `decode_linear_attn`; every other projection remains resident
dequantized.

| Comparison | Value |
|---|---:|
| decode output cosine | `0.999987483` |
| decode output rel_l2 | `0.00507` |
| recurrent state cosine | `0.999999881` |
| recurrent state rel_l2 | `0.000756` |
| conv state cosine | `0.999995768` |
| resident decode latency | `0.562 ms` |
| packed-QKV decode latency | `0.624 ms` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_decode_qkv_probe.json
```

Interpretation:

- Native packed NVFP4 is no longer a standalone benchmark only; it is now
  callable from the incremental decode path.
- One scalar bridge kernel is slightly slower than resident BF16 matmul for a
  single projection, as expected. The value is correctness and plumbing: we can
  replace QKV/Z/B/A/out one by one, then swap the internal kernel for a faster
  FP4 GEMM without changing decode code again.

## P3-D — Decode Linear-Attention Projection Set

The same probe now replaces all linear-attention decode projections:

```text
linear_attn.in_proj_qkv.weight
linear_attn.in_proj_z.weight
linear_attn.in_proj_b.weight
linear_attn.in_proj_a.weight
linear_attn.out_proj.weight
```

| Comparison | Value |
|---|---:|
| decode output cosine | `0.999981105` |
| decode output rel_l2 | `0.00620` |
| recurrent state cosine | `0.999999702` |
| recurrent state rel_l2 | `0.000720` |
| conv state cosine | `0.999995768` |
| resident decode latency | `0.561 ms` |
| packed-projection decode latency | `0.760 ms` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_decode_all_linear_attn_probe.json
```

Interpretation:

- Correctness is stable after replacing all five decode linear-attention
  projections with packed NVFP4 runtime objects.
- The current scalar bridge launches several small kernels and is slower than
  resident BF16 matmul. That is the expected transition state.
- Next performance work should be fuse/batch/tensor-core focused: combine
  projection kernels where possible and replace scalar unpack-dot with a real
  Blackwell FP4 GEMM path.

## P3-E/P3-F — Fusion And MoE Expert Bridge

P3-E validates launch-overhead reduction for tiny projections:

| Probe | Value |
|---|---:|
| separate `linear_attn.in_proj_a` then `in_proj_b` | `0.1015 ms` |
| fused dual `a+b` kernel | `0.0502 ms` |
| speedup | `2.02x` |
| output max_abs vs separate | `0.0` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_dual_ab_probe.json
```

P3-F validates a single packed MoE expert FFN:

| Probe | Value |
|---|---:|
| expert | layer 0 expert 0 |
| packed FFN vs resident cosine | `0.999994755` |
| packed FFN vs resident rel_l2 | `0.00328` |
| resident expert FFN | `0.0650 ms` |
| packed gate/up + packed down FFN | `0.0976 ms` |

Remote reports:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_dual_expert_gate_up_probe.json
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_single_expert_ffn_probe.json
```

Interpretation:

- Packed NVFP4 is now wired into both decode linear-attention and one MoE expert
  FFN path.
- The current kernels are correctness bridge kernels. They prove runtime
  plumbing; they are not the final production FP4 GEMM.
- Small projection fusion already helps (`2.02x`), but production TPS still
  depends on replacing scalar bridge kernels with Blackwell tensor-core FP4 GEMM
  or an equivalent backend.

## P3-G — Full Layer Decode Bridge

P3-G stitches the previous packed pieces into one real layer-0 decode block.
The layer uses packed NVFP4 for:

```text
linear_attn.in_proj_qkv.weight
linear_attn.in_proj_z.weight
linear_attn.in_proj_b.weight
linear_attn.in_proj_a.weight
linear_attn.out_proj.weight
active MoE experts: 2, 15, 55, 65, 127, 131, 157, 223
```

The router and shared expert intentionally stay on the resident dequant oracle
for this milestone. That keeps the probe focused on whether packed
linear-attention projections and packed active experts can be wired into the
real decode layer without changing routing semantics.

| Probe | Value |
|---|---:|
| layer | `0` / `linear_attention` |
| top-k route exact match | `true` |
| final layer output cosine | `0.999997556` |
| final layer output rel_l2 | `0.00218` |
| attention output cosine | `0.999981642` |
| packed MoE same-input cosine | `0.999996305` |
| resident layer decode | `1.7911 ms` |
| packed bridge layer decode | `2.1862 ms` |
| bridge speed ratio | `0.819x` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_layer_decode_packed_probe.json
```

Interpretation:

- Packed NVFP4 is no longer only a projection-level or single-expert probe. It
  now covers a full decode layer: layer norm, linear attention, recurrent state,
  residuals, post-attention norm, top-k MoE, shared expert, and final residual.
- The exact top-k match is important: packed linear-attention noise is small
  enough that this layer's router chooses the same expert order as the resident
  oracle.
- The packed bridge is still slower than resident dequantized BF16 matmul. That
  is expected for scalar bridge kernels. P3-G proves the wiring and correctness
  boundary; P4/P5 should replace the bridge internals with tensor-core FP4 GEMM
  without changing this layer-level contract.

## P3-H — Multi-Seed Router Sensitivity Gate

P3-H repeats the same full-layer packed bridge across eight deterministic hidden
states. This gate catches the subtle class of errors where small packed-path
differences perturb the MoE router at the top-k boundary.

| Probe | Value |
|---|---:|
| seeds | `8` |
| pass count | `8 / 8` |
| top-k exact order match | `7 / 8` |
| top-k set match | `8 / 8` |
| order-only mismatch | `1 / 8` |
| minimum final output cosine | `0.999996960` |
| average final output cosine | `0.999997355` |
| maximum final rel_l2 | `0.00246` |
| minimum route boundary margin | `0.02344` |
| cached packed experts across seeds | `58` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_layer_decode_multiseed_probe.json
```

Interpretation:

- The single order-only mismatch is acceptable and recorded as a warning:
  expert summation is order-invariant, and the selected expert set is identical.
- No seed changed the top-k expert set. That makes the P3-G contract stronger:
  packed linear-attention noise did not push the layer across a routing set
  boundary in this 8-seed gate.
- This is the right regression harness to keep while P4/P5 replaces scalar
  bridge internals with a true FP4 GEMM backend.

## P3-I/P3-J/P3-K — Pre-P4 Hardening

P3-I expands the full-layer packed bridge beyond layer 0. Three representative
linear-attention layers were selected across the depth of the model:

| Layer | Final Cosine | rel_l2 | top-k exact | Packed Bridge |
|---|---:|---:|---:|---:|
| `4` | `0.999996841` | `0.00256` | `true` | `2.3738 ms` |
| `20` | `0.999997079` | `0.00238` | `true` | `2.2036 ms` |
| `36` | `0.999995828` | `0.00289` | `true` | `2.4391 ms` |

P3-J then routes the shared expert through the packed bridge too. The shared
expert is always active, so this closes the last major MoE FFN gap left by P3-G:

| Probe | Value |
|---|---:|
| shared expert mode | `packed-nvfp4-bridge` |
| final layer output cosine | `0.999997556` |
| final layer output rel_l2 | `0.00220` |
| packed MoE same-input cosine | `0.999983609` |
| packed bridge layer decode | `2.2018 ms` |

P3-K is a report-level regression gate over P3-G/H/I/J. It passes only if the
full layer bridge, multi-seed router sensitivity gate, packed shared expert
gate, and cross-layer gate all remain inside threshold.

Remote reports:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_layer_decode_packed_shared_probe.json
/root/autodl-tmp/reports/lynn-engine-p1/p3_nvfp4_cross_layer_bridge_summary.json
/root/autodl-tmp/reports/lynn-engine-p1/p3_regression_gate.json
```

Interpretation:

- P3-I shows the layer bridge is not a layer-0 accident.
- P3-J removes the shared-expert resident shortcut from the MoE FFN path.
- P3-K becomes the safety rail for P4/P5: after swapping scalar bridge internals
  for tensor-core FP4 GEMM, the same report-level gate should still pass before
  any throughput number is trusted.

## P4-A — Native FP4 `torch._scaled_mm` Feasibility

P4-A opens the first native FP4 GEMM path on R6000. It does not yet reproduce
checkpoint numerics; that requires activation quantization and scale repacking.
It proves the runtime/backend channel exists:

| Probe | Value |
|---|---:|
| device | `RTX PRO 6000 Blackwell sm_120` |
| torch | `2.10.0+cu128` |
| dtype | `torch.float4_e2m1fn_x2` |
| backend entry | `torch._scaled_mm` |
| real checkpoint bytes as float4 view | PASS |
| weight | `layer0.linear_attn.in_proj_qkv` |
| shape | `M=1, N=16, K=2048` |
| native FP4 scaled_mm | `0.0170 ms` |
| scalar bridge context | `0.0269 ms` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p4_native_fp4_scaled_mm_probe.json
```

Scale-layout contract discovered:

```text
scale_a_len = max(M, 128) * (K / 16)
scale_b_len = max(N, 128) * (K / 16)
scale dtype = torch.float8_e4m3fn
```

Interpretation:

- The existing compressed-tensors `weight_packed` uint8 storage can be viewed as
  `torch.float4_e2m1fn_x2` without copying.
- PyTorch/cuBLASLt accepts that tensor in `_scaled_mm` on Blackwell.
- The next P4 task is numeric parity: quantize real BF16 activations into the
  same float4 layout and repack compact checkpoint scales into the expanded
  `_scaled_mm` scale contract, then compare against the P3 scalar bridge.

## P4-B — Native FP4 Scale Repack Numeric Gate

P4-B turns the P4-A feasibility channel into a numeric alignment gate. The
checkpoint still stores compact compressed-tensors scales shaped `[out, K/16]`,
but PyTorch's native FP4 `_scaled_mm` expects an expanded, swizzled
`float8_e4m3fn` scale vector.

The discovered scale index contract for PyTorch 2.10 on R6000 sm_120 is:

```text
idx = tile * (128 * groups)
    + (group // 4) * 512
    + (row % 32) * 16
    + (row // 32) * 4
    + (group % 4)

where:
  tile   = row // 128
  group  = K-group index, one scale per 16 logical K values
  groups = max(K / 16, 4)
```

This was derived from single-nonzero synthetic FP4 matrices and then validated
on a real checkpoint tensor:

| Layout | Cosine | rel_l2 | Mean Abs |
|---|---:|---:|---:|
| row-major guess | `0.929642` | `0.423261` | `0.667210` |
| group-major guess | `0.919338` | `0.432884` | `0.744304` |
| **torch swizzled** | **`0.994396`** | **`0.106489`** | **`0.180193`** |

Probe details:

| Item | Value |
|---|---:|
| weight | `layer0.linear_attn.in_proj_qkv` |
| shape | `M=1, N=128, K=2048` |
| activation | synthetic FP4, `scale_a=1` |
| reference | explicit unpack + compressed-tensors v8 scale dequant |
| native path | `torch._scaled_mm(float4_e2m1fn_x2, float8_e4m3fn scales)` |
| verdict | `PASS` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p4_native_fp4_numeric_probe.json
```

Interpretation:

- The previous row-major/group-major layouts were wrong; their cosine stays
  around `0.92-0.93`.
- The swizzled layout is the correct bridge between compressed-tensors compact
  scales and PyTorch/cuBLASLt's native FP4 scale contract.
- The remaining numeric gap is expected because `_scaled_mm` requires runtime
  scales to be quantized to `float8_e4m3fn`, while the explicit reference keeps
  scales in FP32.
- This closes the native weight-scale repack part of P4. The next step is real
  BF16 activation quantization into `float4_e2m1fn_x2`, then replacing the P3
  scalar bridge internals with this native path behind the same layer gates.

## P4-C — BF16 Activation Quantization Into Native FP4

P4-C adds the activation side of the native FP4 GEMM contract. A deterministic
BF16 activation matrix is quantized into `torch.float4_e2m1fn_x2` storage with
one scale per 16 hidden dimensions, then both activation scales and weight
scales are repacked into the PyTorch/cuBLASLt swizzled `float8_e4m3fn` layout.

Probe result:

| Item | Value |
|---|---:|
| weight | `layer0.linear_attn.in_proj_qkv` |
| shape | `M=8, N=128, K=2048` |
| activation scale rule | `amax(abs(group)) / 6.0` |
| activation packed dtype | `torch.float4_e2m1fn_x2` |
| native scale dtype | `torch.float8_e4m3fn` |
| mean abs | `0.0001309` |
| max abs | `0.001953` |
| rel_l2 | `0.000335` |
| cosine | `0.999999881` |
| verdict | `PASS` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p4_native_fp4_activation_quant_probe.json
```

Interpretation:

- P4-B showed the native weight-scale repack is numerically aligned.
- P4-C shows BF16 activation quantization and activation scale repack also
  align with `torch._scaled_mm`.
- The explicit reference uses the same FP8-rounded runtime scales as native
  `_scaled_mm`, so this gate isolates native GEMM correctness from the separate
  question of quantization quality vs original BF16 activations.
- P4-D can now replace `PackedNVFP4Linear`'s scalar bridge with this native
  scaled-mm path and rerun P3-K as the safety rail.

## P4-D — Opt-In Native Scaled-MM Runtime Backend

`PackedNVFP4Linear.forward()` now exposes an opt-in runtime backend:

```python
linear(x, backend="native_scaled_mm")
```

The default backend remains the P3 scalar bridge. This keeps the existing P3
regression gates stable while making the native FP4 path callable through the
same runtime object.

Single-projection probe:

| Item | Value |
|---|---:|
| weight | `layer0.linear_attn.in_proj_qkv` |
| input dtype | `bf16` |
| scalar bridge latency | `0.0518 ms` |
| native scaled-mm latency | `1.4335 ms` before P4-E vectorization |
| cosine vs scalar bridge | `0.987262` |
| rel_l2 vs scalar bridge | `0.159422` |
| verdict | `PASS` |

Multi-seed sanity distribution for the same projection:

| Seeds | Cosine Range | rel_l2 Range |
|---|---:|---:|
| `1,2,3,4,5,20260514` | `0.987262-0.991719` | `0.128628-0.159422` |

Remote report:

```text
/root/autodl-tmp/reports/lynn-engine-p1/p4_native_fp4_runtime_wrapper_probe.json
```

Interpretation:

- P4-D is an integration gate, not a throughput gate.
- The lower cosine relative to P3 is expected because `native_scaled_mm`
  quantizes activations to FP4, while the scalar bridge keeps BF16 activations
  and only consumes packed FP4 weights.
- The current implementation still does activation quantization and scale
  swizzling in Python per call, so it is slower than the scalar bridge today.
- The engineering path is now clear: keep the runtime API stable and move
  activation quantization + scale swizzle into a fused native kernel or lower
  overhead backend before trusting TPS.

## P4-E — Vectorized Scale Swizzle Hot-Path Cleanup

The first P4-E cleanup removes the Python double loop from
`_compact_scale_to_swizzled_fp8()`. The same empirical swizzle formula is now
applied with vectorized tensor indexing.

This does not change numerics:

| Metric | Before | After |
|---|---:|---:|
| cosine vs scalar bridge | `0.987262` | `0.987262` |
| rel_l2 vs scalar bridge | `0.159422` | `0.159422` |
| native scaled-mm latency | `1.4335 ms` | `0.3567 ms` |

Interpretation:

- P4-E confirms one real Python overhead source was the per-call scale swizzle
  loop.
- Native scaled-mm is still slower than the scalar bridge at `M=1` because the
  backend still quantizes activations per call and launches a small GEMM shape.
- The remaining performance work is to fuse activation quantization and/or batch
  projections so the Blackwell FP4 tensor-core path sees enough work per launch.

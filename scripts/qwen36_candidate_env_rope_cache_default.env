# Stream B Task 1 candidate: flip LYNN_FULL_ATTN_ROPE_CACHE default to "1".
#
# The cache itself moved out of engine/incremental_decode.py into
# triton_kernels/full_attn_rope_cache.py on 2026-05-18 (claude/phase-a-foundation,
# commit 3a2f1a0). The default is still "0" in the safe route. This candidate
# turns it on so the promotion gate can verify whether the owned-module path
# stays exact-greedy + 40/40 structured + >=108 TPS at P25 512-token decode.
#
# Promotion-gate status on R6000, 2026-05-18: NOT YET RUN
# - P37 exact-greedy: pending
# - P25 512-token decode TPS: pending
# - hard structured gate: pending
# - decision: pending
#
# Run with:
#
#   CANDIDATE_NAME=rope_cache_default \
#   CANDIDATE_ENV_FILE=scripts/qwen36_candidate_env_rope_cache_default.env \
#   scripts/r6000_qwen36_candidate_promotion_gate.sh
#
# DEFAULT promotion thresholds (per 2026-05-18 hand-off):
#   P37 3/3 exact AND structured 40/40 AND P25 512 >=108 TPS
LYNN_FULL_ATTN_ROPE_CACHE=1
